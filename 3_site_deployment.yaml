apiVersion: apps/v1
kind: Deployment
metadata:
  name: site-deployment
  labels:
    app: site
spec:
  selector:
    matchLabels:
      app: site
  template:
    metadata:
      labels:
        app: site
    spec:
      containers:
      - name: site
        image: wordpress:php8.1-apache
        ports:
        - containerPort: 80
